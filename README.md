# Auctionary Website
Auction website for University Fullstack module

# Requirements
In terminal do 'npm install' inside both Backend folder and Frontend folder respectively to install node modules in order to run.
Then run 'npm run dev' inside both Backend and Frontend aswell in order to run the server alongside the website Frontend

# Changes

Made Backend recognised as a module instead of being recognised as a submodule
